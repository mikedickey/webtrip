[toolchain]
# Pinned to match the build container (Containerfile RUST_NIGHTLY). Bump both together.
channel = "nightly-2026-06-13"
components = ["rust-src"]
profile = "minimal"
targets = ["wasm32-unknown-unknown"]
